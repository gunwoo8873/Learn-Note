// Variable name role
// 1. can be specified using strings, numbers, or special characters.
// 2. need to first words is string or special characters. = [variable, _variable]
// 3. Case sensitive[구분] strings
// 4. A declared[선언] variable can have values, and each value has one data type

string strValue;
strValue = "Small";

Console.WriteLine(strValue);

string StrValue;
StrValue = "Big";

Console.WriteLine(StrValue);

int integerValue = 0; // New variable declared integer type value
integerValue = 15; // Declared variable value to change new value update

Console.WriteLine(integerValue);